import Navbar from '@/components/Navbar/Navbar';
import React from 'react';

const MainLayout = () => {
    return (
        <div>
            <Navbar/>
        </div>
    );
};

export default MainLayout;